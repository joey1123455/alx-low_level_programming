/**
* print_rev - print rev of str
* @s: str to rev
*
* return: void
*/

#include "main.h"

void print_rev(char *s)
{
	int x;
	int y;

	while (s[x] != '\0')
		b++;
	for (y = x - 1; y >= 0; y--)
		_putchar(s[y]);
	_putchar('\n');
}
